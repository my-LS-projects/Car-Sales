// index.js files help you "index" and organize all the files in a single directory
// One place to export all
// we can now import from the directory instead of the file

export { carReducer } from './carReducer'